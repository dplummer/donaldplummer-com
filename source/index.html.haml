- blog.articles[0...5].each_with_index do |article, i|
  %article{:class => (i == 0) ? 'first' : ''}
    %h1
      %a{:href => article.url}= article.title
    %time.updated{:updated => article.date.strftime("%Y-%m-%d")}= article.date.strftime('%b %e %Y')
    = article.body
  %hr
